apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-analyzer.fullname" . }}
  labels:
    {{- include "inference-analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.app.type }}
  selector:
    {{- include "inference-analyzer.selectorLabels" . | nindent 4 }}
  ports:
    - name: app
      port: {{ .Values.service.app.port }}
      targetPort: app
      protocol: TCP
