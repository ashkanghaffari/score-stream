apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-console.fullname" . }}-management
  labels:
    {{- include "admin-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.management.type }}
  selector:
    {{- include "admin-console.selectorLabels" . | nindent 4 }}
  ports:
    - name: management
      port: {{ .Values.service.management.port }}
      targetPort: management
      protocol: TCP
