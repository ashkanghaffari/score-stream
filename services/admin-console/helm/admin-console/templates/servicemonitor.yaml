{{- if .Values.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "admin-console.fullname" . }}-management
  labels:
    {{- include "admin-console.labels" . | nindent 4 }}
    release: prometheus
spec:
  selector:
    matchLabels:
      {{- include "admin-console.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: management
      path: actuator/prometheus
      interval: {{ .Values.serviceMonitor.interval }}
{{- end }}