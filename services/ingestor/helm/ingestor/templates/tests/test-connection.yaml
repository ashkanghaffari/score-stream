apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingestor.fullname" . }}-test-connection"
  labels:
    {{- include "ingestor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingestor.fullname" . }}:{{ .Values.service.app.port }}']
  restartPolicy: Never
