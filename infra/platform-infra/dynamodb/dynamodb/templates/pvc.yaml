apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "dynamodb.fullname" . }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
