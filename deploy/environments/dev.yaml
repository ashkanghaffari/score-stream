namespace: dev
repoOwner: ashkanghaffari  # linked to GH org/user
images:
  admin-console: latest
  ingestor: latest
  ingestion-gateway: latest
  inference-analyzer: latest
image:
  pullPolicy: Always
